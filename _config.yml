title: LukyDrum
author:
  name: LukyDrum
  email: lukydrum42@proton.me

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  Articles about coding, game development and more...

plugins:
  - jekyll-feed
  - jekyll-seo-tag

highlighter: rouge
markdown: kramdown
kramdown:
  syntax_highlighter: rouge
  input: GFM
